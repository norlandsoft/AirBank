# AirBank 培训环境变量模板：复制为 .env 后可按需修改
IMAGE_TAG=latest
TZ=Asia/Shanghai

# PostgreSQL（培训环境固定值，勿用于生产）
PG_PASSWORD=AirBank@2026
PG_HOST_PORT=5432

REDIS_HOST_PORT=6379
NACOS_NAMESPACE=airbank-dev
NACOS_HOST_PORT=8848

GATEWAY_HOST_PORT=8080
COUNTER_WEB_PORT=8001
EBANK_WEB_PORT=8002
GRAFANA_PASSWORD=admin

# 后端服务调试端口（仅发布到 127.0.0.1，供 swagger 直连调试）
UAM_HOST_PORT=8083
CORE_HOST_PORT=8081
WEALTH_HOST_PORT=8082
COUNTER_HOST_PORT=8084
EBANK_HOST_PORT=8085
